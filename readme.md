# Счетчик поссещения страниц на flask (python)
### Зависимости
- python 3.9
- redis
- прокси перед приложением (nginx)

### Сборка и запуск
```
pip install --no-cache-dir -r requirements.txt
python app.py
```
